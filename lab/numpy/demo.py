import numpy as np
import time

# before = time.time()
# a = np.random.rand(1000000)
# b = np.random.rand(1000000)
# c = np.dot(a, b)
# after = time.time()
#
# print(str(1000 * (after - before)) + "ms")

print(np.random.rand(*(4, 2)))
